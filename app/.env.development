# Loaded automatically when you run `npm run dev` (NODE_ENV=development).
# DB name `mangosteen` is the shared Mangosteen database (not app-specific).
# Override in `.env.local` if needed (that file is gitignored).
MONGO_URI=mongodb://localhost:27017/mangosteen
