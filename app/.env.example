# Raw env var (intentionally insecure for the exercise — not Secret Manager).
# Local dev: `.env.development` is committed with a default URI, or copy this file to `.env.local`.
MONGO_URI=mongodb://localhost:27017/todoiz
